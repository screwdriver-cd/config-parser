parameters:
  music: [jazz, rock]

jobs:
  main:
    template: JobParametersTestNamespace/jobparameterstemplate@2
    description: "This is the main description"
    parameters:
      color: [red, blue]
