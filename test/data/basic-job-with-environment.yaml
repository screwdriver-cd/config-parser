shared:
    environment:
        A: "hello"
        B: ${A}
        FOO: ${B}
        BAZ: "world"
jobs:
    main:
        image: latest-image
        template: mynamespace/environment@1.2.3
        description: "This is the main description"
        environment:
            A: "meow"
            B: ${A}
            FOO: ${B}
    publish:
        image: node:4
        description: "This is the publish description"
        steps:
            - publish: "npm publish"
            - teardown-mystep: "echo bye"
