parameters:
  music: [jazz, rock]

jobs:
  main:
    image: "node:18"
    description: "This is the main description"
    parameters:
      color: [red, blue]
      sports:
        "value": ["football", "basketball"]
        "description": "favorite sports"
      car: "Audi"
    steps:
      - install: npm install
