jobs:
  main:
        image: node:6
        description: "This is the publish description"
        steps:
            - publish: "npm publish"
            - publish: "echo duplciate step"
            - teardown-mystep: "echo bye"
