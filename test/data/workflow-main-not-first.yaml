workflow:
    - foobar
    - main

jobs:
    main:
        image: node:4
        steps:
            install: npm install
            test: npm test
            publish: npm publish

    foobar:
        image: node:4
        steps:
            install: npm install
            test: npm test
